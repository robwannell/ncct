<h1>Gallery</h1>
    <% @albums.each do |album| %>
      
        <h3><%= link_to album.title, album %> <small><%= link_to 'Edit', edit_album_path(album) %> | <%= link_to 'Destroy', album, method: :delete, data: { confirm: 'Are you sure?' } %></small></h3>
     
    <% end %>
 
<br>




<% content_for :rh do %>
<%= render "/layouts/shop_ad.html.erb" %>

<% if current_user %>


<div class="well">
<span class="glyphicon glyphicon-info-sign info"></span><h4>Add a new Album</h4>

<%= link_to 'New Album', new_album_path, class: 'btn btn-default'%>
</div>
<% end %>
<% end %>

